<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    @page { size: A4; margin: 1cm; }
    body {
      display: flex;
      font-family: 'Verdana', sans-serif;
      font-size: 9pt;
      margin: 0;
    }
    .left {
      width: 30%;
      background: linear-gradient(to bottom, #00c6ff, #0072ff);
      color: #fff;
      padding: 16px;
    }
    .right {
      width: 70%;
      background: #ffffff;
      padding: 16px;
      color: #2c3e50;
    }
    h1 { font-size: 16pt; margin: 0 0 6px; }
    h2 { font-size: 10pt; border-bottom: 1px solid #ccc; color: #0072ff; margin: 10px 0 4px; }
    h3 { font-size: 10pt; color: #ffffff; margin: 10px 0 4px; }
    ul { padding-left: 16px; margin: 0 0 6px; }
    .skills-grid { list-style: none; padding: 0; }
    a { color: #e1ffff; }
  </style>
</head>
<body>
  <div class="left">
    <h1>{{ Contact.Name }}</h1>
    <p>{{ Contact.Email }}<br>{{ Contact.Phone }}<br><a href="{{ Contact.LinkedIn }}">LinkedIn</a></p>
    {% if Skills %}<h3>Skills</h3><ul class="skills-grid">{% for skill in Skills %}<li>{{ skill }}</li>{% endfor %}</ul>{% endif %}
    {% if Certification %}<h3>Certifications</h3><ul>{% for c in Certification %}<li>{{ c }}</li>{% endfor %}</ul>{% endif %}
  </div>
  <div class="right">
    {% if Summary %}<h2>Summary</h2>{% for s in Summary %}<p>{{ s }}</p>{% endfor %}{% endif %}
    {% if Experience %}<h2>Experience</h2>{% for e in Experience %}<p><strong>{{ e.Role }}</strong> – {{ e.Company }} ({{ e.Duration }})</p><ul>{% for d in e.Description %}<li>{{ d }}</li>{% endfor %}</ul>{% endfor %}{% endif %}
    {% if Projects %}<h2>Projects</h2>{% for p in Projects %}<p><b>{{ p.Name }}</b> ({{ p.Year }})<br>{{ p.Description }}</p>{% endfor %}{% endif %}
    {% if Education %}<h2>Education</h2>{% for e in Education %}<p>{{ e.Degree }} – {{ e.Institute }} ({{ e.Year }})</p>{% endfor %}{% endif %}
  </div>
</body>
</html>
